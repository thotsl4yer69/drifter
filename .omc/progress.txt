RALPH RUN 2026-04-22 — Pull latest, override local, remove bloat, deploy node
================================================================================

US-001 — Override local repo with origin/main  [DONE]
  - Stashed transient .omc/ changes, then `git reset --hard origin/main`.
  - Local diverged at 826ee59; local-only commits (fe85275, 23e84bb, 702575b,
    91ab14b — vivi/flipper feature work) replaced by upstream 09cbe9b
    (fleet-contract layer + diagnose CLI).
  - HEAD == origin/main == 09cbe9b. No tracked-file modifications.
  - vivi.py, flipper_bridge.py absent from working tree (deleted upstream).
  - scripts/oneshot.sh, src/diagnose.py, bin/drifter, tests/test_diagnose.py,
    CLAUDE.md present.

US-002 — Stop and remove obsolete vivi/flipper services  [DONE]
  - systemctl stop + disable drifter-vivi, drifter-flipper.
  - Removed /etc/systemd/system/drifter-{vivi,flipper}.service.
  - daemon-reload completed.

US-003 — Purge obsolete bloat from /opt/drifter  [DONE]
  - Removed /opt/drifter/vivi.py, /opt/drifter/flipper_bridge.py.
  - vivi.yaml was already absent.
  - No orphan __pycache__ entries for vivi/flipper remained.

US-004 — Run fleet-contract oneshot deploy  [DONE]
  - First pass with --skip-apt left /opt/drifter on STALE code (Apr-21 config.py
    still listed vivi/flipper in SERVICES). LEARNING: --skip-apt skips
    install.sh entirely, including its src copy step, so /opt/drifter is not
    refreshed. Workaround: manually deploy src/*.py to /opt/drifter before
    re-running --skip-apt.
  - chmod +x scripts/post-deploy-check.sh (fix: stage 30 require -x bit).
  - Manually deployed all 28 src files + screen_dash.html + start-hud.sh +
    knowledge_base.json + realdash/drifter_channels.xml; chowned to drifter.
  - Re-ran sudo bash scripts/oneshot.sh --skip-apt:
      STAGE 10 OK / 20 OK / 30 OK / 40 OK / FINAL OK → DEPLOY: ok
  - At deploy time /healthz returned 200 with services_failed:[] and full
    services list (15/15 active including drifter-voicein at boot).

US-005 — Verify drifter node is healthy  [DONE]
  - 14/14 hardware-independent services active:
      canbridge, alerts, logger, anomaly, analyst, voice, hotspot, homesync,
      watchdog, realdash, rf, wardrive, dashboard, fbmirror.
  - drifter-voicein exits cleanly (status=0/SUCCESS) with message
    "No wake word model AND no GPIO — no trigger method available, exiting"
    when no audio hardware/RPi.GPIO is present. Matches its pre-deploy
    inactive-dead state. NOT a regression.
  - drifter-vivi / drifter-flipper inactive (intentional removal).
  - drifter CLI installed at /usr/local/bin/drifter.
  - md5 verified: /opt/drifter/{config.py, diagnose.py, web_dashboard_handlers.py,
    screen_dash.html} all match repo src.
  - post-deploy-check.sh: 40 passed / 3 warnings / 0 failed (warnings = audio,
    can0, RTL-SDR — all USB hardware not plugged in per user).
  - /healthz returns 200 immediately after deploy; transitions to
    503 "degraded" with services_failed:["drifter-voicein"] once voicein
    self-exits ~3s after startup. This is the contract correctly reporting
    that the node has incomplete capability without audio hardware.

ENVIRONMENT NOTES
  - User: USB hardware (CAN2USB, RTL-SDR, mic) not plugged in yet.
  - Once hardware is connected: can0 will come up via 80-can.rules,
    rtl_433 will start receiving, and drifter-voicein can be revived
    by installing a wake-word model into /opt/drifter/openwakeword/ or
    plugging in a GPIO PTT button.
  - paho-mqtt v2 on_connect signature warning visible in voicein traceback —
    cosmetic only, voicein exits before MQTT loop becomes load-bearing.

LEARNINGS FOR FUTURE RALPH RUNS
  - oneshot.sh --skip-apt does NOT redeploy src files to /opt/drifter.
    For a code-only refresh use install.sh stage 7, or manually cp the
    SRC_FILES list (defined in install.sh:215) before --skip-apt re-run.
  - post-deploy-check.sh ships without +x bit; chmod it after a fresh clone.
  - /healthz uses a 2s server-side cache; observe state for at least 5s
    before declaring deploy success/failure.
